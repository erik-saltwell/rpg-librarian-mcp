# Copy to .env at your library root (the directory you start Claude Code
# from) and fill in values as needed. Loaded automatically on server start;
# a missing .env is fine -- nothing here is required yet.
#
# Reserved for future config, e.g. API keys for external product-lookup
# tools (ISBN/RPGGeek/DriveThruRPG/STL marketplaces) once those are built.
